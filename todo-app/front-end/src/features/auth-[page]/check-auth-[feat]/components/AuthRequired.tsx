export default function AuthRequired() {
    return (
        <div>
            <h1>Authentication Required</h1>
            <p>Please log in to access this page.</p>
            {/* You can add a login form or redirect to the login page here */}
        </div>
    );
}