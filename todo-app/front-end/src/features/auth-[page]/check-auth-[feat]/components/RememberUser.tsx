export default function RememberUser() {
    return (
        <div>
            <h1>Remember User</h1>
            <p>This component is responsible for remembering the user.</p>
            {/* You can add logic to remember the user, such as storing user data in local storage or cookies */}
        </div>
    );
}